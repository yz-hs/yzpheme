hexo-config-uq(config)
    $ret = hexo-config(config)
    $ret = unquote($ret)
    return $ret

@import "_variable/main";
@import "_other/main";

if hexo-config-uq('WHITE')=="WHITE"
    @import "_WHITE/main";
else
    @import "_partial/main";
    @import "main";